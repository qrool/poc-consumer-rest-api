{
    "name": "qrool/poc-consumer-rest-api",
    "description": "Fetching and manipulate fake Json data obtained from REST API. This example is using Docker, Nginx, PHP 8.0 (fpm), Composer and Sqlite to work",
    "type": "project",
    "require": {
        "php": "^8.0",
        "ext-pdo": "*",
        "guzzlehttp/guzzle": "^7.3"
    },

    "minimum-stability": "stable",

    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
